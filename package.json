{
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.6"
  },
  
  "scripts": {
    "start": "node index.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  }
}

